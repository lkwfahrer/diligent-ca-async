import { getRandomNumber, sleep } from "../promises.js";

/*
  Implement a function that uses the getRandomNumber to
  get a random number async, then wait the given number
  of seconds. 

  But the whole function have a 3 sec cap. It should stop
  in any case after 3sec.
*/

const task = () => {

}

task()